name: Test the Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    name: Test the Pull Request
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: |
          npm ci
          npm run build
          npm run start
          npm run test
