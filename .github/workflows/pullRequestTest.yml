name: Test the Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    name: Test the Pull Request
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install
        run: |
          install: npm ci

      - name: build
        run: |
          build: npm run build

      - name: start
        run: npm run start

      - name: test
        run: npm run test
